name: Asset Sync (Local Excel)

on:
  schedule:
    # 每6小时执行一次 (北京时间 2:00, 8:00, 14:00, 20:00)
    - cron: '0 */6 * * *'
  
  workflow_dispatch:
    # 支持手动触发

jobs:
  sync:
    name: Sync Assets from Local Excel
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.14'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pandas openpyxl
      
      - name: Sync data from local Excel
        run: |
          echo "🚀 开始同步资产数据"
          python3 sync_assets.py
      
      - name: Commit and push
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add src/data.json
          git diff --staged --quiet || git commit -m "🔄 自动同步资产数据 [skip ci]"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Create summary
        run: |
          echo "## 📊 资产同步完成" >> $GITHUB_STEP_SUMMARY
          echo "✅ 数据已从本地Excel同步到data.json" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "📅 执行时间: $(date)" >> $GITHUB_STEP_SUMMARY
